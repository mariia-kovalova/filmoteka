import { Header } from 'components/Header';
import { SearchForm } from 'components/SearchForm/SearchForm';

export const Home = () => {
  return (
    <Header>
      <SearchForm />
    </Header>
  );
};
